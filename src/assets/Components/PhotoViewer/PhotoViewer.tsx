import './PhotoViewer.css';

const PhotoViewer: React.FC = () => {
    return (
        <section className="photo-viewer">
            <div className="photo-viewer-placeholder">
                Photo Viewer Component
            </div>
        </section>
    );
};

export default PhotoViewer;